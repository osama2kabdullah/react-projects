import React from 'react';
import Form from './Form';

const Register = () => {
    const position = true;
    return (
        <div>
            <Form position={position}></Form>
        </div>
    );
};

export default Register;